name: top_toast
description: >-
  Flutter toast, snackbar and confirmation dialog package. Top-sliding
  overlay toasts with light/dark mode, 6 positions, 3 animations,
  queue/stack, progress bar, and six dialog types with async confirm.
version: 1.0.0
homepage: https://github.com/dhruvin2004/top_toast
repository: https://github.com/dhruvin2004/top_toast
issue_tracker: https://github.com/dhruvin2004/top_toast/issues
topics:
  - toast
  - snackbar
  - notifications
  - dialog
  - overlay

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
